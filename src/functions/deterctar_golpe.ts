import { Arbol } from '../classes/arbol';
import { Venado } from '../classes/venado';

export function detectarGolpe(/* flecha:Flecha, flechaPosicion:number, arrayObj:( Arbol | Venado)[] */) {
  
  /* for (let i = 0; i < arrayObj.length; i++) {
    let objeto = arrayObj[i];

    if( flecha.posicionX + flecha.ancho >= objeto.posicionX &&
        flecha.posicionX <= objeto.posicionX + objeto.ancho &&
        flecha.posicionY <= objeto.posicionY &&
        flecha.posicionY <= objeto.posicionY + objeto.alto
    ){
      this.flechas.splice(this.flechas[flechaPosicion], 1);
      arrayObj.splice(i, 1);

      if(arrayObj instanceof Venado){
        sumarPuntaje();
      }
    }
  }  */ 

}
/* 
let puntaje = 0;

function sumarPuntaje(){
  puntaje ++
  document.querySelector(".puntaje").innerHTML = "Puntaje: " + puntaje;
} */